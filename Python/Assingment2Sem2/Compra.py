class Compra:

    def __init__(self, dataCompra, produto, fornecedor, qntdCompra, precoUnit):
        self.dataCompra = dataCompra
        self.produto = produto
        self.fornecedor = fornecedor
        self.qntdCompra = qntdCompra
        self.precoUnit = precoUnit